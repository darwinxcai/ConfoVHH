# Authorized execution 03 operations

The user authorized 45 additional cumulative GPU minutes and directed completion of the same frozen pilot. The cumulative ceiling is 11,100 seconds (3h05m), with 7,485.271 seconds already counted conservatively and 3,614.729 seconds available. Seven minutes are reserved for preservation and shutdown. No scientific setting or original runtime timeout is changed.

The retained pod is resumed at the verified $1.59/hour compute rate. Its original provider creation identity remains fixed; the resumed session is counted from a conservative timestamp no later than the start request. A separate deadline file records the resulting absolute cutoff. A standalone Python process uses the provider-injected pod-scoped credential to request stop one minute before the hard ceiling, independently of this chat or SSH. An additional detached local guard may use the same pod-scoped credential; neither guard can terminate data. Successful read authorization alone is not presented as proof of write permission.

The SSH entrypoint reuses an existing validated SSH package closure and public key when present, or invokes the original base-preserving bootstrap under a new output directory when the container was reset. The existing environment is reused after file/package verification if present; otherwise the exact checksum-verified captured bundle is restored. There is no dependency resolution or rebuild. Historical restore receipts remain historical and bind to their original provider-base receipt; a new environment binding records actual resume verification.

Fresh original GPU runtime and full Triton JIT checks precede the unmodified frozen generation runner. The runner retains its own original internal check. The detached controller proceeds directly from passing checks to the two original seeds and ten planned samples. It stops on failure or the workload cutoff, records every slot and preserves partial outputs. No replacement candidates are authorized.

The remote controller automatically creates a checksum manifest and archive after generation or failure. A detached local collector downloads the original receipt, archive and manifest and checks every tar member and extracted file before declaring backup success. Root stops and terminates the pod only after the needed files are verified locally. If a connection failure prevents local verification before the deadline, the independent guard stops compute and retains disk; it never deletes unverified outputs.

Operational source hashes, actual provider identity, absolute deadlines and the unchanged source freeze are recorded in the execution's launch manifest. Previous executions and their receipts remain unchanged. Local frozen ConfoVHH scoring and DockQ evaluation follow preservation, including the first available candidate's API/CLI crosscheck and all exact ties, failures and missing values.
