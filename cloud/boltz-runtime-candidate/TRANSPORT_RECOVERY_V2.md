# Transport recovery v2 — shared temporary filename correction

Scope: additive operational recovery of the already approved, unchanged runtime bundle during execution `confovhh-3p0g-startup-recovery-20260909-01`. This amendment changes transport reconstruction only. No model input, seed, scoring rule, dependency lock, compiler package, runtime checker, generation runner or previous receipt changes.

The failed v1 transfer helper is preserved with SHA-256 `61983592f5ae492f274cd54dacd940cb680ed3e76344c06966b324ca8fbcf940`. Its three concurrent extraction workers use `part.with_suffix('.extracting')`. For each of `runtime-bundle.part00` through `runtime-bundle.part07`, this replaces the numbered suffix and produces the **same** temporary pathname, `runtime-bundle.extracting`. Workers open it with truncating `wb` mode, write concurrently and rename the shared pathname to different final parts. The observed missing-file rename error is consistent with another worker having already renamed it. Even a matching hash accumulated from a ZIP input stream cannot certify the bytes eventually present at a raced destination. All v1 extracted parts therefore remain untrusted pending separate file verification; do not reuse their stream-based success labels as preservation proof. ZIP download temporary names retain their part number and do not have this collision.

Recovery v2 must retain all v1 logs, receipts, extracted files and verified source ZIPs untouched. Work in a new directory. For each of the eight ZIPs, recheck its size and SHA-256 against the independently preserved artifact registry, require exactly its expected single regular member, and verify that member's advertised size. Reconstruct parts **sequentially**, using distinct names that retain the entire part name (for example `runtime-bundle.part00.extracting`) and exclusive creation. Close each file, then reread the stored file to verify its actual byte count and SHA-256 against the frozen transport manifest before promoting it. Record the ZIP identity, member identity, final-file identity and execution order. Do not redownload, rebuild or change bundle bytes when the preserved ZIPs verify.

Concatenate exactly the verified parts `00` through `07` into a new-only archive. Require **3,570,472,960 bytes** and SHA-256 `4a2c2862c100ae1dc49a886b3533b6da23e80678a7323bc450b4946d0d98471f`; reread the completed stored archive before declaring success. Promote it to the preparation path only if that path is absent. Preserve any previous partial archive instead of overwriting it. Subsequent bundle verification still requires manifest SHA-256 `b2ea93126a22099ba7861dbd4822b29e075739c028f47934eebb98db6e6caa3c` and the original environment's complete file verification.

The original Boltz CLI timeout remains **60 seconds**, and the frozen runner's enclosing runtime check remains **180 seconds**. Original runtime PASS and full fresh GPU Triton JIT PASS are still required before any prediction. This recovery is not a runtime-gate retry or inference attempt, and does not authorize another ten-candidate execution.

The cumulative allowance does not reset. The existing allocation began at **2026-09-09 20:49:06.508 UTC**; 3,915.779 earlier GPU seconds plus at most 4,484.221 seconds for this allocation remain bounded by the approved **8,400-second total**. The workload cutoff stays **21:56:50.729 UTC**, normal stop target **22:02:20.729 UTC**, independent watchdog stop target **22:02:50.729 UTC**, and absolute allocation deadline **22:03:50.729 UTC**. Transport recovery consumes that same remaining interval and monetary budget. If reconstruction or later preparation fails or cannot fit, preserve evidence and stop rather than extending a limit or silently changing the experiment.

Independent review confirmed the eight-to-one temporary-path collision directly from the preserved v1 source. Reported on-pod stream successes and the rename failure are controller observations; v2's own receipts must establish the recovered final-file and archive identities before preparation resumes.
