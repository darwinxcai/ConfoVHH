# SSH key filesystem amendment v1

The first archived-cache v2 receive attempt failed before source user authentication and transferred no payload. The coordinator observed that the temporary target private key on the retained `/workspace` MFS mount still reported mode `0666` after a request to set `0600`. OpenSSH rejected the accessible private key. The receive-side empty-stream failure was a consequence; the original SSH stderr identifies the cause.

The failed attempt remains separate under `retained-incoming-v2`, including its original `receipt.json`, SSH stderr and failure diagnostics. These original records must accompany the final preservation evidence without rewriting them as success. This local amendment records the coordinator's demonstrated cause and the launch-manifest entry; it does not replace the raw remote receipt or claim a separate remote inspection.

The operational correction moves the **same one-use transfer key** to the target's local filesystem under `/opt/retained-transfer-access` and verifies its actual mode is `0600` before retrying. The coordinator verified actual mode `0600` on `/opt`, removed the old workspace private-key copy, and verified the source payload claim was still unused before the v3 receive began at 22:42:32 UTC. The corresponding public key, source forced-command restriction and pinned source host key remain unchanged. SSH permission checking, source authentication, host-key verification, one-use claim and complete file/stream checks remain enabled. No private key contents are included in this amendment, the launch manifest or experiment archive.

The next receive uses a new `retained-incoming-v3` quarantine directory; it never reuses or overwrites the failed v2 output. The source forced command was not reached during the rejected-key attempt, so its payload claim was not consumed. Successful receipt verification and no-overwrite promotion are still required before the execution03 resume controller creates `/workspace/executions`.

This changes only operational key placement and the new receive output path. It changes no source/input/model bytes, dependency, base image, runtime/JIT checker, seed, candidate, scoring rule or deadline. The original 60-second child help and 180-second inner runtime checks remain unchanged. The workload cutoff remains 2026-09-09 23:21:49.747930 UTC and the cumulative GPU hard stop remains 23:28:49.747930 UTC, with the existing preservation reserve. The source CPU shutdown remains under the coordinating controller's independently bounded recovery allocation.
