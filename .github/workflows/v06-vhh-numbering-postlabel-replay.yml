name: V0.6 VHH numbering post-label replay

on:
  push:
    branches:
      - accuracy/vhh-numbering-v06-candidate-20260901

permissions:
  contents: write

concurrency:
  group: v06-vhh-numbering-postlabel-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-postlabel-replay:
    if: ${{ !contains(github.event.head_commit.message, 'Record v0.6 VHH numbering post-label replay') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out candidate branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - name: Use Node.js 22.18
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22.18.0
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install frozen dependencies
        run: npm ci
      - name: Execute the preregistered post-label development replay
        run: node scripts/run-v06-vhh-numbering-postlabel-replay.mjs
      - name: Verify and record the derived replay artifact
        run: |
          artifact="validation/v0.6-vhh-numbering-candidate-v1/postlabel-replay.json"
          test -s "$artifact"
          git add "$artifact"
          git diff --cached --check
          changed="$(git diff --cached --name-only)"
          if [ -z "$changed" ]; then
            echo "Post-label replay artifact already matches the reproducible run."
            exit 0
          fi
          test "$changed" = "$artifact"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Record v0.6 VHH numbering post-label replay"
          git push origin "HEAD:${GITHUB_REF_NAME}"
