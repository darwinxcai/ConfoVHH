# Publication-directed continuation, 8 September 2026

Starting repository state: merged PR #53, main
`4895c46c63194f032b83b69d2e5cbcf23a9719d2`. The user requested progress toward a
publishable paper. The near-term route is a software/application contribution
with the existing negative selection result stated clearly. Submission is not
yet ready; an independent ranking-performance claim remains unsupported.

## Completed in this increment

- Fixed the Boltz workflow's unconditional CIF template key: PDB inputs now
  receive the PDB key, CIF/mmCIF retain the CIF key, and unsupported formats
  reject. Explicit chain pins are preserved; no guessed chain identities or
  frozen-input changes are made.
- Executed the unmodified pinned official Boltz parser on generated synthetic
  input in a partial CPU environment. Retained the processed masks/mappings,
  input bytes, three failing controls, exact wheel/source hashes, package
  versions, and [replay instructions](../scripts/paper/boltz-parser-README.md).
  The PDB author chain `X` becomes `X1`; one missing atom is masked; the expected
  arginine naming correction is retained. Real templates remain unchecked.
- Added a runnable production-parser/audit/export demonstration with an
  independent distance oracle, separated control, export round trip and altered
  result rejection. Its [receipt](evidence/reviewer-demo-2026-09-08.json) and
  [reviewer guide](REVIEWER_GUIDE.md) preserve the execution and limitations.
- Added a [software-paper draft](SOFTWARE_PAPER.md),
  [submission gates](SUBMISSION_READINESS.md), and an uncompleted
  [independent reviewer task sheet](WORKFLOW_EVALUATION.md). Tightened the README
  interpretation of correlated native-derived perturbations and replaced the
  stale undated test total with commit-specific CI.

## Continue from here

Run the documented reviewer workflow against the proposed submission release;
obtain a real independent completion before claiming researcher usability.
Resolve durable permitted access and redistribution terms for the retained
application inputs, then bind input manifest, commands, environment and outputs
to a stable release/archive. Missing 85 preliminary cognate coordinates and full
PAE must remain explicit; a retained-subset paper need not pretend those were
recovered. Author metadata, contributions, funding/COI and project-wide AI
disclosure need the author's factual completion before submission.

The current no-native-coordinate/no-prediction-output restriction prevents
checking real templates or rerunning application outputs here. Synthetic parser
success does not waive that boundary or satisfy the production parsing/cache
gates. No model weights, production chemical cache, MSA service, paid compute,
or external participant contact was used for this increment.

The formally cleared independent eligible-group count is unchanged at **0**.
Scientific census dispositions, exposure records, frozen inputs, leakage gates
and target-freeze status are unchanged. Further census work is useful only where
it can supply defensible new source evidence or independent evaluation; another
exclusion tally alone does not establish paper readiness.

## Follow-up: transient public-download failures

The publication PR's hosted public-producer regression encountered HTTP 504
while retrieving a pinned public input. A separate follow-up replaces its
single-attempt download with at most three attempts for transient network errors
and HTTP 429/502/503/504, with a 60-second timeout per attempt and bounded
backoff/Retry-After handling. A server-requested delay above the allowed budget
stops the request. Each attempt uses the same source URL. Byte-count, SHA-256,
and strict UTF-8 checks remain mandatory; content mismatches are never retried.

Twelve synthetic regressions cover recovery, attempt exhaustion, timeouts,
permanent failures, retry timing, and provenance failure. This changes download
reliability only; it does not update dataset pins, scientific results, or census
dispositions. PR #55 merged at `9aeadb8185a02da4e67967d9fbe12b1503d02e96`
after all 733 tests, exact public-producer replay, Node 22/24 release gates,
coverage, browser/accessibility and Vercel checks passed.

## Claim/evidence and data-availability package

Seven central manuscript statements are now mapped to 14 exact public evidence
artifacts in [CLAIM_EVIDENCE.json](CLAIM_EVIDENCE.json). The verifier checks each
SHA-256, confirms the claim text remains present, requires every artifact to
support a claim, and rejects promotion of synthetic checks, removal of required
missing-input records, superiority claims, eligibility-count changes or target
freeze. This is a traceability improvement, not new scientific evidence.

The companion [data-availability record](DATA_AVAILABILITY.md) separates public
software/synthetic replay, committed derived-result replay, and unavailable
raw inputs. It supplies provisional manuscript wording without inventing a
custodian, permission, archive or DOI. Durable original-input access,
independent researcher completion, author disclosures and final archival release
remain open.

## Follow-up: source-specific download pressure

The claim/evidence PR's public-producer gate twice received HTTP 403 from
different files in the same Zenodo record after concurrent requests. The exact
file pins did not change, and 403 remains non-retryable. The runner now retrieves
that record serially while retaining concurrency four for the separately hosted
GitHub dataset. A reusable batch helper preserves input order, accepts at most
128 files and caps concurrency at four. Synthetic tests measure peak concurrency
at one and four and reject invalid or unbounded work.

This is a transport-pressure change only. Exact URL, byte count, SHA-256 and
UTF-8 validation remain mandatory. It supplies no new producer, biological,
paper-result, eligibility or census evidence.

Serial retrieval still received HTTP 403 on the first API-content request. The
runner therefore uses the same Zenodo record's canonical public record-file
download route instead of the API-content route. The constructor accepts one
bounded filename component and no credentials. The hosted exact-file regression
must still verify every existing size and SHA-256 before this transport change
can merge.

## Publication follow-up: reviewer exports and exact software methods

The current main was rechecked at
`9aeadb8185a02da4e67967d9fbe12b1503d02e96`; PR #56 remained open. The
publication work now includes an [exact methods supplement](SOFTWARE_METHODS.md)
with geometry-tier cutoffs, units, missingness handling, shortlist ordering,
ensemble ordering and immutable source identities. The paper entry point now
prioritizes reproducibility and manuscript availability ahead of optional
prediction extensions. Python 3 is documented as a release-test prerequisite.

The [reviewer task sheet](WORKFLOW_EVALUATION.md) has an executable export
comparison. The verifier checks the current source identities, regenerates the
synthetic reference, invokes the production report importer, and compares both
reports field by field except their export timestamps. It rejects swapped cases,
tampered measurements, policy/missingness changes and edited reference receipts.

The [preserved browser execution](evidence/reviewer-browser-2026-09-08/README.md)
completed both uploads, role confirmation, replacement/reset, audits and
downloads. The two browser reports reproduce the generated references exactly
under that comparison. The new browser regression observes unavailable PAE,
pLDDT and CDR annotation, and no off-origin requests or page errors. It runs in
the existing hosted acceptance job. This is automated synthetic verification,
not a participant's completion or research-use evidence. Its receipt distinguishes
local source identities from attestation of a complete served build.

A separate downloader defect was fixed: after the first batch failure, no
queued requests start; already active requests drain before the first error is
returned. Deterministic deferred-promise tests cover stopped dispatch, draining,
later errors and falsey synchronous failures. All file pins, allowed retry
statuses and content checks remain unchanged.

The bounded local checks passed: 28 focused unit regressions, preserved-browser
comparison replay, the complete synthetic browser workflow, and the seven-claim
manifest verification. Full local release and hosted gates are required before
merge. The prior head's non-network gates passed, including a successful retry
after a V8 optimizer SIGTRAP. A later attempt of its exact public-producer gate
still received HTTP 403 from Zenodo on the first pinned file. No access or
provenance gate was weakened, and no changed source bytes were substituted.

Continue by checking PR #56's current head and all required CI gates. When the
public source serves the pinned bytes again, merge only after the exact-byte
regression passes. Preserve the unfilled independent-reviewer task sheet and
the original-input availability gaps. A publication can state a retained-subset
application only with accurate availability and limitations; software test
counts do not establish efficacy. Author facts/disclosures, independent
researcher participation and final archive/venue decisions remain outstanding.
The independent eligible-group count is still **0**; no census disposition,
frozen study input, exposure decision or eligibility gate changed.

## Paired-selection evaluation component

The user next requested progress toward accurately assessing GPCR–nanobody
complexes. The current main remains `9aeadb8185a02da4e67967d9fbe12b1503d02e96`;
PR #56's reviewed publication tree is at
`12603c70e1d638feb208c47292840481d23df5cd`. Its 749 tests on Node 22/24,
adversarial suites, coverage, browser/accessibility and static build pass; its
exact producer gate still receives HTTP 403 from Zenodo.

The new [paired-selection component](PAIRED_SELECTION_EVALUATION.md) implements
expected top-1 success with uniform scientific ties, one paired baseline,
generator/target/group averaging, paired hierarchical percentile intervals and
leave-one-group-out effects. It retains failed/ineligible attempts and refuses
aggregates with missing eligible strata. Supplied inventory/rank/outcome IDs
must reconcile exactly. An external pre-label inventory and policy adapter
attestation remain necessary: declared groups and hashes are not certificates.

The preserved example is synthetic arithmetic only. Its two group effects are
+0.5 and -1; the macro difference is -0.25. Neither method is the product score,
and these numbers must not enter the paper's scientific results. The component
does not change any ranking policy, open real outcomes, certify independent
groups or implement the complete v3 endpoint battery. Independent eligible
groups remain zero. Continue with authorized data availability, formal grouping
and frozen rank/outcome exports before any real comparison; do not adapt the
score to the retained application and then call that validation.

Ten focused synthetic regressions cover exact accounting, uniform ties,
unequal group sizes, generator averaging, paired uncertainty, exhaustive small
resampling distributions, input rejection, strict JSON and preserved receipt
replay. Full required hosted checks must pass on the new head before merge.
