# Frozen source retrieval follow-up

A metadata-only Git fetch for the exact protocol-declared commit failed with
`upload-pack: not our ref`. The command explicitly excluded file blobs. This
adds a Git-transport observation to the earlier API lookup; it does not prove
that no private or archived copy exists. No alternative commit or tree was
substituted, and no frozen execution gate was cleared.

The receipt timestamp is its recording time, not a reconstructed server time.
The remaining requirement is the exact declared source identity, plus a
source-backed interpretation of raw-feature quantization. Formatting numbers
for display cannot establish the scientific rounding operation.
