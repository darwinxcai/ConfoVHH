# Readiness-navigation exposure record

While locating the next executable evaluation task on 8 September 2026,
`paper/README.md` was opened in full. That navigation page contains aggregate
development selection/outcome summaries and case-specific development outcome
prose. This event is retained without reproducing the values here.

The viewed file's SHA-256 was
`635249a1c4c5af263e3deef812938b7c88c46a5ef5cd1e52f4f4e487722a66f6`.
No linked coordinate file, real prediction report, structural contact table or
per-pose outcome ledger was opened through that navigation. Subsequent report
integration tests use generated synthetic reports only; the GPR158 comparison
uses permitted sequence and canonical-residue metadata.

The read event does not grant those summaries new scientific authority or
establish eligibility, independence, a clean-blind state or exposure clearance.
No product score or frozen protocol was tuned against the returned prose.
Earlier exposure records remain intact. Formal exposure adjudication remains
required under the existing study rules.

## Repeated source-header exposure during the next continuation

During a source-only audit of the unresolved frozen ranking contract, a separate
code-review agent read `lib/pose-ranking.ts`, including its existing aggregate
development-summary header. The reported observation window was approximately
17:55–18:00 UTC on 8 September 2026; an exact read timestamp was not retained.
The file SHA-256 was
`6f0be122edcf7e6e8a70ea0f562dc0e1aa14dff4adf19b2bf2f67794711d0911`.

This is an additional access event, even though the same source-header category
was already recorded in the ranking-policy review. No native coordinate file,
real prediction output, relative pose, contact table or per-pose outcome ledger
was opened in this audit. Values from the header are not reproduced or used to
choose parameters. The new prequantized-tuple utility is derived from the
existing frozen contract and synthetic ordering examples; it does not establish
frozen-engine identity, a rounding rule, a clean-blind state or exposure
clearance. Existing records and the need for formal adjudication remain intact.
