# ConfoVHH product 0.9.1

Product 0.9.1 strengthens the public scientific boundary around the still-unexecuted hard-decoy benchmark while preserving the attested v0.5.0 scientific-core source, executed `immunum 1.2.0` bytes, and every frozen historical artifact. Its dependency/build environment is separately security-patched and is not represented as the historical v0.5 lockfile.

> **Deployment safety:** this release does not publish the credential-bearing CI `dist` bundle. Its production-build manifest is inspection/attestation-only and cannot be deployed. Deploy from the annotated source tag with a fresh verified build, which mints new Vinext framework credentials.

## Independent-evidence preparation

- Selects and checksums a v3 sealed one-way native-epitope oracle design without claiming that its request, independent parser/container, key ceremony, leakage graph, target set, or benchmark has been frozen or executed.
- Adds strict canonical-JSON, transcript, commitment, fixed-size encrypted-evidence, request-package, and fail-closed integrity controls with adversarial regression tests.
- Archives two repeated raw captures of the four-term RCSB/GPCRdb source sub-universe: 2,065 RCSB entries, 1,716 GPCRdb entries, exact API/HTML agreement, and a 287-entry intersection. Every row remains pending scientific disposition.
- Preserves two independently timed 24-response entry-metadata captures (48 raw responses total). Both replay to the exact same 287 entries, 1,401 polymer entities, and 287 triage rows; the 39/242/6 strata are non-dispositive signals, not target eligibility.
- Rejects duplicate JSON object keys before semantic scanning in both metadata collectors and replay verifiers, including escaped-equivalent overwrite attacks and rechecksummed raw-response mutations.
- Replaces full-repository Stage-A workflow checkouts with SHA-pinned, token-scoped materialization of exact 33-file and 67-file read-only metadata closures. Receipts bind every Git blob, SHA-256, byte count, and ordered root; broader project and historical evaluation artifacts are never materialized into those jobs.
- Records RCSB CC0 1.0 and GPCRdb CC BY 4.0 source-license mappings and live evidence URLs, plus import and integration receipts; the external license-page bytes are not archived.
- Makes the first annotation-epitope draft explicitly archival and nonauthoritative; only the selected sealed native-contact oracle may establish formal epitope independence.
- Preserves the terminal v2 census and records the separate leakage-component-out development-evaluation design as unexecuted and resource-gated.
- Keeps independent-holdout preparation and the target census blocked at seven provisional groups, zero formally cleared groups, and an unchanged minimum of ten; no hard-decoy DockQ/CAPRI label or holdout performance result was accessed during v3 preparation.
- Expands production-mode Chromium acceptance to 4/4 scenarios. The added end-to-end case fetches only the exact release-pinned 3P0G RCSB file, verifies its byte count and SHA-256, audits the A/B interface to the frozen 46-contact / zero-clash / 1,729 Å² visible result, checks accessibility and the non-binding warning, and validates the provenance-bound JSON download.
- Replaces the previously published exact-built-production archive with a deterministic path/size/SHA-256 manifest. The release publishes no server-bundle credential bytes, fails if its output directory contains stale assets, and requires a fresh build from the annotated source tag for deployment.

No binding, affinity, specificity, functional-state, membrane-compatibility, candidate-selection, near-native-ranking, or hard-decoy holdout claim is added.

---

# ConfoVHH product 0.9.0

Product 0.9.0 improves research usability, evidence integrity, and release traceability while preserving the digest-attested v0.5.0 scientific engine and every frozen validation artifact.

## Research workflow

- Replaces automated retain/deprioritize recommendations with neutral coordinate-geometry flags. These flags are not validated candidate-selection, ranking, or experimental-hit rules.
- Preserves legacy wording inside the canonical v0.5 engine record for reproducibility while mapping it to neutral language in the current product display and researcher handoff brief.
- Clears researcher-authored dispositions whenever a prediction run is recomputed and binds exported decisions to exact coordinate, audit-result, PAE, and topology evidence identifiers.
- Clarifies first-use navigation, qualified evidence labels, export names and contents, mobile limitations, and the experimental worked-example boundary.
- Verifies the exact byte count and SHA-256 of the remotely fetched worked example before parsing it.

## Integrity and accessibility

- Neutralizes CSV formulas even when trigger characters follow leading whitespace or Unicode format characters.
- Adds response-level content security, framing, referrer, MIME-sniffing, and permissions headers.
- Rejects non-read HTTP methods before framework routing and publishes a dated triage of installed dependency advisories without weakening the frozen engine attestation.
- Improves supporting-text size and contrast and adds a real Chromium/WCAG A/AA acceptance gate.
- Makes post-audit and post-ensemble focus deterministic after React commits the rendered results. At the product 0.9.0 release, the production-mode Chromium suite passed 3/3 scenarios, including the complete local coordinate-audit and report-download path; product 0.9.1 extends that gate to 4/4 with the release-pinned 3P0G audit/export above.
- Adds measured core coverage floors and CI on Node 22.18 and Node 24.

## Release traceability

- Adds full-SHA-pinned GitHub Actions, exact producer-output regression as a distinct check, and a CI-gated product-release workflow.
- At the product 0.9.0 release, assets included normalized source and exact-built-production archives, a CycloneDX SBOM, provenance receipt, and `SHA256SUMS`. Product 0.9.1 no longer publishes the credential-bearing production archive; it publishes a deterministic exact-output hash manifest instead. Neither release claims independently recompiled Vinext bundles are byte-identical because the framework injects fresh build credentials.
- Documents that legacy attestation commits resolve as ancestors in the Sites source history but are absent from the current public GitHub repository, while their recorded digests remain verifiable and are bound to the reachable product tag.

No binding, affinity, specificity, functional-state, membrane-compatibility, candidate-selection, near-native-ranking, or hard-decoy holdout claim is added.
